version: "3.9"

services:
  emailgen:
    build: .
    container_name: smart-email-generator
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - ./credentials.json:/app/credentials.json:ro
      - ./token.pkl:/app/token.pkl
    restart: unless-stopped
